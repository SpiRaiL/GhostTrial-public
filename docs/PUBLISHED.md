# What is published here, and what is not

Generated by `tools/make_public.py` from the working repo.

**242 files, 254.6 MB.**

## Included

| area | files | what it is |
|---|---|---|
| `.` | 2 | README and .gitignore |
| `data` | 136 | motion data: the capture solved to human BVH, retargeted to G1, frozen CSV takes and the motion_lib pickles SONIC trains on |
| `docker` | 2 | the Isaac Lab image the training and export run in |
| `docs` | 6 | methodology: the capture brief, the video-to-pose plan, the training environment |
| `raw` | 5 | the one clip authored by hand, plus READMEs pointing at sources held back |
| `tools` | 85 | the pipeline, end to end: retarget, retime, balance, feasibility checks, figures, training and eval jobs |
| `policy` | 6 | the fine-tuned SONIC policy exported to ONNX, with the config it trained under |

## Held back

| what | files | why |
|---|---|---|
| `a5n.csv` | 1 | scratch file, no longer referenced |
| `data/gemx_output/*/preprocess/` | 12 | per-frame ViT features, boxes and camera solved straight off the performer's video |
| `presentation/` | 1615 | the video and everything staged for it: MK stills, the soundtrack, 3.6 GB of Pit frames, BlenderKit scene assets |
| `raw/mixamo/*.fbx` | 3 | Mixamo assets are free to use but not to redistribute; raw/mixamo/README.md says which clips and how to fetch them |
| `raw/mixamo/bvh/` | 2 | converted straight from those Mixamo FBX, so the same terms |

Also never tracked in the first place, so absent from both trees: the 1992 behind-the-scenes footage, the commissioned capture and every frame cut from it, the soundtrack, the Nebius and Mixamo screen recordings, the reports, and the training checkpoints.
